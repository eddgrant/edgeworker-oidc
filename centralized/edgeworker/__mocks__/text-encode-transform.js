import {vi} from 'vitest'

export const TextEncoderStream = vi.fn().mockImplementation(() => {
    return {};
});

export const TextDecoderStream = vi.fn().mockImplementation(() => {
    return {};
});